# Examples, Instructions, and Structured Outputs

## Argument
Context engineering reduces the output space through tool formatting and structured outputs — constraining what the model can produce to what the system can reliably parse.

## Why it matters (reliability goal)
Tie to Pragmatics pillar: show the model exactly what you want (examples), tell it explicitly (instructions), and constrain how it responds (structured outputs).

## Beats

1. **Few-Shot as Teaching** — evidence: [Tool use as few-shot](research/toolformer-notes.md), [DSPy few-shot selection](research/dspy-notes.md)
   - Good example: exact input format, expected output format, reasoning pattern
   - Bad example: ambiguous, complex, or wrong behavior
   - Few-shot = teaching the reasoning pattern, not just the format

2. **Tool Formatting** — evidence: [Tool call format training](research/toolformer-notes.md)
   - Models trained on specific tool call formats — need explicit instruction
   - Best practices: schema-first, one-shot examples, failure handling
   - Ambiguity → wrong function names, malformed arguments, non-existent tool calls

3. **Structured Outputs** — evidence: [NER evaluation metrics](research/knowledge-graphs/ie-ner.md)
   - Constrain to JSON, XML, typed objects — parseable, validated, auditable
   - Patterns: JSON Schema forcing, grammar-based generation, two-stage extract-then-validate
   - Function calling as earliest reliable agent pattern

4. **Measuring Extraction Performance** — evidence: [NER precision/recall/F1](research/knowledge-graphs/ie-ner.md)
   - Same metrics apply: Precision, Recall, F1, Slot Error Rate
   - Makes prompt performance observable and optimizable

## Conclusion to reach
The reader should believe that reliability comes from reducing ambiguity at every layer: examples show patterns, instructions state constraints, and structured outputs bound the response space.

## Examples needed
- Code: schema-first tool definition

## Readiness
✓ ready (content exists, citations verified)