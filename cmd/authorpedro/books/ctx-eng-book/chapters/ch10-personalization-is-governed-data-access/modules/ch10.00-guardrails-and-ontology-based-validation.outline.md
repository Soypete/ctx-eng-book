# Guardrails and Ontology-Based Validation

## Argument
Guardrails and context work in tandem — context reduces hallucination by providing relevant information, while guardrails validate output against structured knowledge to ensure the model stays within defined bounds.

## Why it matters (reliability goal)
**Pillar:** Governance
**Reliability Goal:** Prevent hallucinations, ensure output validity, maintain semantic consistency
**Failure Mode:** Models generate plausible but incorrect, invalid, or unauthorized outputs

## Beats

1. **The Two Halves of Reliability** — evidence: existing prose
   - Context supplies the model with relevant information (the "what to talk about")
   - Guardrails constrain what the model can say and do (the "what not to say/do")
   - Neither alone is sufficient — you need both

2. **Ontology as Guardrails** — evidence: ledger (relation-extraction, "Shift Left for Ontology Design")
   - An ontology defines what entities and relations are valid
   - Extract only what the ontology allows
   - Validate every extraction against ontology constraints

3. **Knowledge Graph as a Guardrail** — evidence: ledger (Zaveri et al. KG quality, OWL standard)
   - The knowledge graph IS a set of constraints
   - Query the graph to validate proposed outputs
   - Graph traversal can validate relationships before they're used

4. **Types of Guardrails** — evidence: existing prose + ledger (SHACL, OWL standards)
   - Entity constraints — Only allow entities that exist in the ontology
   - Relation constraints — Only allow relationships defined in the schema
   - Output schema validation — Ensure structured outputs match expected formats

5. **Context + Guardrails Synergy** — evidence: existing prose
   - More context = fewer guardrails needed (model has relevant info)
   - More guardrails = less context needed (model is constrained)
   - The optimal balance depends on use case stakes

## Conclusion to reach
Guardrails and context are complementary — context provides relevant information, guardrails filter invalid outputs. Together they form the governance layer that makes AI systems reliable.

## Examples needed
- Guardrail validation code that queries a knowledge graph
- Ontology-based entity validation
- Output schema enforcement with graph lookup

## Readiness
ready | needs-research | needs-decision

**Evidence status:** Strong evidence from ledger entries:
- [Vaswani et al., 2017](https://arxiv.org/abs/1706.03762) — "Attention solves routing, not truth"
- [relation-extraction research](research/knowledge-graphs/relation-extraction.md) — "Ontologies are foundational for RE"
- [Zaveri et al., 2015](https://doi.org/10.3233/SW-150176) — KG quality dimensions including provenance
- [W3C OWL Standard](https://www.w3.org/TR/2012/REC-owl2-overview-20121211/) — Ontology modeling for constraints

## State of the Stack (to add to .md)
- JSON Schema, SHACL, OWL standards
- Neo4j, GraphDB, PostgreSQL constraints
- Guardrails (Confident AI), Rebuff, NVIDIA NeMo, Anthropic built-in