# Toolformer and ReAct

## Argument
Tool use gives a model a structured proposal space; the host creates the action boundary through parsing, semantic validation, authorization, execution limits, and result handling.

## Why it matters (reliability goal)
Tools are pragmatic/action contracts: they expose a bounded proposal space while the host retains lexical authority, semantic interpretation, identity and scope, validation, authorization, and execution. Without structured interfaces, language systems operate in unbounded text space; with them, proposals become inspectable and rejectable.

## Beats

1. **The Action Problem** — evidence: existing prose
   - Text generation is unbounded; tool use creates bounded action space
   - Tool definitions = pragmatic context; inputs = constrained semantic context; outputs = structured, provenance-bearing observations

2. **Toolformer Origins** — evidence: existing prose + ledger (tool usage patterns)
   - Toolformer generated and filtered API-call data, then fine-tuned the model
   - Runtime tool descriptions influence a tool-capable model but are not the Toolformer training method
   - Learned proposal behavior is not host-side authority

3. **Modern Tool Calling** — evidence: existing prose
   - First-class in OpenAI, Anthropic, Google APIs
   - Pattern: define contract → model proposes → host resolves scope and validates → authorize → execute → return scoped, fresh, provenance-bearing observation

4. **State of the Stack** — evidence: existing prose
   - JSON Schema as a common representation, not a complete semantic or authorization contract
   - Infrastructure: LangChain, LlamaIndex, LangGraph, AutoGen

## Conclusion to reach
The reader should distinguish a model's structured tool proposal from the host's enforced authority and execution boundary.

## Examples needed
- Diagram: text generation vs. tool-calling action space
- Code: simple tool definition across providers

## Readiness
✓ ready (content exists, citations verified)

## Citations
- [Schick et al., 2023](https://arxiv.org/abs/2302.04761) — Toolformer paper
- [Yao et al., 2022](https://arxiv.org/abs/2210.03629) — ReAct paper
