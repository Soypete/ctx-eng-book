# Tokens, Embeddings, and Attention

## Argument
Tokens, embeddings, and attention form the foundation of what models see. Understanding these mechanisms is prerequisite to engineering reliable context—because if you don't know what a token is, you can't reason about what the model sees.

## Why it matters (reliability goal)
Make the context engineering framework concrete at the model
boundary. Syntax is the representation through which available lexicon and
semantic structure reach the process; it is not semantics itself. What enters
attention can influence output, but authority, meaning, and permitted use are
not supplied by tokenization alone.

## Beats

1. **Tokens Are Not Words** — evidence: existing prose
   - Subword tokenization varies across models
   - Same text = different token counts across model families
   - Implications: context limits, pricing, retrieval boundaries

2. **Token Representations Are Not Retrieval Embeddings** — evidence: primary embedding source needed
   - Token IDs map to input embeddings; layers produce contextual representations
   - Retrieval embeddings use model- and objective-specific pooling or encoders
   - Similarity is a proxy for relevance, not identity, authority, or freshness

3. **Attention Combines Permitted Positions** — evidence: Vaswani et al. (2017); long-context evidence gap marked
   - Queries, keys, and values combine information across heads and layers
   - Autoregressive masks constrain which positions can influence a token
   - Long-context and distractor effects are empirical and task-dependent, not equal competition among all tokens

4. **The Core Distinction: Representation, Attention, and Context Engineering** — evidence: research/attention-is-all-you-need-notes.md
   - Syntax makes supplied information processable; it does not decide what it means or who may use it
   - Attention transforms information already available to the invocation
   - Context engineering governs sources, meaning, authority, and downstream use before and after the model call

## Conclusion to reach
The reader should distinguish tokenization, internal representations, retrieval
embeddings, and attention, then inspect the serialized input and context
manifest—including source authority and provenance—when behavior fails.

## Examples needed
- Diagram: tokenization of same sentence across models
- Diagram: attention as competition (not additive)

## Readiness
needs-research (long-context distractor evidence and a primary retrieval-embedding source are needed)
